<Window x:Class="Translator.Views.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource SurfaceBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        FontFamily="{StaticResource UiFont}"
        UseLayoutRounding="True"
        Icon="/Assets/app.ico">
    <StackPanel>
        <StackPanel Margin="24,18,24,20">
            <TextBlock x:Name="HeadingText" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"/>
            <TextBlock x:Name="MessageText" Margin="0,10,0,0" FontSize="13" TextWrapping="Wrap" LineHeight="20"
                       Foreground="{DynamicResource TextSecondaryBrush}" AutomationProperties.AutomationId="MessageText"/>
        </StackPanel>
        <Border Background="{DynamicResource WindowBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,1,0,0" Padding="24,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SecondaryButton" Style="{StaticResource SecondaryButton}" IsCancel="True"
                        AutomationProperties.AutomationId="DialogSecondaryButton"/>
                <Button x:Name="PrimaryButton" Style="{StaticResource PrimaryButton}" IsDefault="True" Margin="10,0,0,0"
                        AutomationProperties.AutomationId="DialogPrimaryButton" Click="PrimaryButton_Click"/>
            </StackPanel>
        </Border>
    </StackPanel>
</Window>
